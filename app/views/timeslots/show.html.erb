<h2>Timeslot</h2>

<table>
  <tr>
    <th>Date</th>
    <td><%= @timeslot.recording_session_date.strftime("%B %-d, %Y") %></td>
  </tr>
  <tr>
    <th>Start time</th>
    <td><%= @timeslot.start_time.strftime("%l:%M%P") %></td>
  </tr>
  <tr>
    <th>End time</th>
    <td><%= @timeslot.end_time.strftime("%l:%M%P") %></td>
  </tr>
</table>

<br />
<%= link_to "Back", root_path %>